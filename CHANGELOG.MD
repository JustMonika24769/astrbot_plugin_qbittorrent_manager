# Changelog

所有重要更改都将记录在此文件中。

## [1.6.1] - 2026-08-18

### qBittorrent 兼容性

- 兼容 qBittorrent 5.2 登录成功时返回 `204 No Content` 的 WebAPI 行为。
- 保留对旧版本 `200 OK` 和 `Ok.` 响应正文的支持。
- 兼容 qBittorrent 5.2 添加任务时返回的计数 JSON、`202 Accepted` 和 `409 Conflict`。
- 区分账号密码错误、登录被拒绝和无法识别的响应，避免将登录成功误报为密码错误。

### 版本

- 将插件发布版本号提升至 `1.6.1`。

## [1.4.0] - 2026-07-25

### 搜索结果状态

- 解析 NexusPHP 搜索结果中的当前用户种子状态和完成进度。
- 在结果卡片中以“左侧状态文字、中央进度条、右侧百分比”的形式显示下载、做种、完成或暂停状态。
- 下载进度使用蓝色、做种使用绿色、非活动状态使用灰色；无状态数据时保持原有卡片布局。
- 纯文本回退结果同步显示当前状态和进度。

### 版本

- 将插件发布版本号提升至 `1.4.0`。

## [1.3.1] - 2026-07-24

### 搜索排序

- 修复 AstrBot 将搜索命令参数截断为第一个单词，导致排序选项被当作搜索词的问题。
- 兼容全角等号、全角短横线、中文破折号等中文输入法常见符号。

### 版本

- 将插件发布版本号提升至 `1.3.1`。

## [1.3.0] - 2026-07-24

### 搜索排序

- `/种子` 支持通过单条命令按时间、做种人数、下载人数、完成人数、大小或标题排序。
- 支持升序和降序；未指定顺序时默认降序，未传排序参数时保持 PT 站默认顺序。
- 使用独立的 `--` 分隔排序参数与搜索词，避免带空格或特殊符号的种子名称被误解析。
- 排序条件会传递给 NexusPHP 站点，并对返回结果进行本地稳定排序和重新编号。

### 版本

- 将插件发布版本号提升至 `1.3.0`。

## [1.2.0] - 2026-07-12

### 下载功能

- `/种子下载` 支持一次选择多个搜索结果，例如 `/种子下载 1 3 5`。
- 支持使用空格、英文逗号或中文逗号分隔序号，并自动去除重复序号。
- 执行下载前统一校验全部序号；存在非数字或越界序号时整批拒绝，不会产生部分下载任务。
- 下载客户端请求阶段逐条处理任务，并汇总返回成功与失败结果。

### AstrBot 兼容性

- 管理员权限判断兼容 `is_admin()` 方法、`is_admin` 属性和 `role` 字段，避免部分 AstrBot 版本或平台事件中命令静默失效。

### 文档

- 新增 `CHANGELOG.MD`，记录插件各版本的重要改动。
- 更新 README、命令帮助、插件元数据和搜索结果卡片中的批量下载说明。

### 版本

- 将插件发布版本号提升至 `1.2.0`。

## [1.1.0] - 2026-07-10

### 权限控制

- 新增插件用户白名单和黑名单，支持按 QQ 号限制插件使用权限。
- 新增 AstrBot 管理员绕过开关，管理员身份由 AstrBot 配置的管理员 ID 判断。
- 新增全局配置仅管理员可用开关；普通用户可改用个人配置和插件内置默认值。
- 权限检查覆盖搜索、下载、帮助和个人配置命令。

### 版本

- 将插件发布版本号提升至 `1.1.0`。

## [1.0.0] - 2026-07-10

### 多用户配置

- 按 QQ 号隔离用户的 PT 站点、Cookie、下载客户端和搜索结果缓存。
- 新增 AstrBot 面板用户配置列表，支持为不同 QQ 用户维护独立配置。
- 新增 `/种子配置` 命令组，支持查看、设置、删除和重置个人配置。
- 保留原有配置作为全局默认值，个人配置优先覆盖，兼容旧版本部署。
- Cookie 和密码在机器人配置查看结果中使用掩码显示。

### 安全

- 外部 HTTP(S) 种子链接不再携带 PT Cookie；仅同源 PT 链接会附带登录 Cookie。

### 测试

- 新增解析、多用户配置、配置持久化和安全边界相关单元测试。

### 版本

- 将插件发布版本号提升至 `1.0.0`。

## [0.1.1] - 2026-07-10

### 搜索解析

- 修复 NexusPHP 搜索页面外层布局行被误识别为首条种子的问题。
- 修复首条搜索结果做种、下载和完成数量错误显示为 `0` 的问题。
- 修复首条结果简介混入“搜索箱（点击收起/展开）”文本的问题。

### 版本

- 将插件发布版本号提升至 `0.1.1`。

## [0.1.0] - 2026-07-09

### 搜索与下载

- 支持在 NexusPHP 风格 PT 站点搜索种子，并通过 AstrBot t2i 渲染搜索结果卡片。
- 支持按搜索结果序号下载 `.torrent` 文件并添加到 qBittorrent。
- 支持 qBittorrent WebUI 登录、保存路径、分类和暂停选项。
- 支持 uTorrent WebUI Token、种子文件上传和磁链添加。
- 支持直接添加 magnet 磁链和 HTTP(S) 种子链接。
- 按用户和会话缓存搜索结果，并在卡片渲染失败时回退纯文本结果。

### 配置与文档

- 提供 AstrBot 配置 schema、安装说明、使用说明和安全提示。

### 版本

- 发布首个版本 `0.1.0`。
